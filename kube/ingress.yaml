apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-mvi
spec:
  rules:
  - host: mviapp.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mvi-analyst-f-service
            port:
              number: 80
  ingressClassName: nginx