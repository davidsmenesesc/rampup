apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-movie
spec:
  ingressClassName: nginx
  rules:
  - host: mviapp.com
    http:
      paths:
        backend:
          serviceName: mvi-analyst-f-service
          servicePort: 3030
---