pipeline {

  environment {
    dockerimagebackend = "movie-analyst-api"
    dockerImage = ""
    dockerimagefrontend = "movie-analyst-ui"
  }

agent any

stages {

    stage('Checkout Source') {
      steps {
        git 'https://github.com/davidsmenesesc/rampup.git'
      }
    }
    stage('Build image') {
      steps{
        script {
          dockerBack = docker.build dockerimagebackend
          dockerFront = docker.build dockerimagefrontend
        }
      }
    }
 }
}